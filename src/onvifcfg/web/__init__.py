"""Local FastAPI web UI for onvifcfg.

Started from the CLI via `onvifcfg serve`. Binds to localhost by default,
so it's intended for the user's own machine - exposing it on a network
would let anyone with reach reconfigure cameras.
"""
